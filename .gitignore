# ── Misc ──
.symlinks
.ignore/
ignore*
#   Project-specific: *ignore/, src/

# ── Dependencies ──
node_modules/
vendor/

# ── Build output ──
build/
dist/

# ── PHP — general ──
.phpunit.result.cache
.php_cs.cache
.php-cs-fixer.cache
phpstan.neon
#   Track phpstan.neon.dist instead (distributed config, local override is ignored)

# ── JavaScript / TypeScript / Node ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/
.npm/
tsconfig.tsbuildinfo

# ── Archives & databases ──
*.zip
*.tar.gz
*.sql
*.diff
*.err
*.rej
*.vi

# ── OS files ──
.DS_Store
Thumbs.db
._*
*.orig

# ── Editor / IDE ──
.idea/
.vscode/
*.swp
*.swo
*~
*.sublime-project
*.sublime-workspace
*.code-workspace
.project
.settings
.tmproj
*.esproj
nbproject

# ── Temp / cache ──
*.tmp
*.cache
*.log

# ── Env ──
.env
.env.local
.env.*.local
